<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">

<style>
    body {
    font-family: 'Inter', sans-serif;
    background: rgb(255, 232, 219);
    background-attachment: fixed;
    background-repeat: no-repeat;
    color: #333;
    margin: 0;
    padding: 0;
  }

    .navbar {
    background: linear-gradient(135deg, #4f9aff, #56d8ff);
    border: none;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  } 

    .navbar-brand,
    .nav-link {
    color: white !important;
    font-weight: 600;
    transition: color 0.3s ease;
  }

    .nav-link:hover {
    color: #dceeff !important;
  }

    body.profile-page {
    background: linear-gradient(to bottom, #fdfdfd, #ffe3d8);
    min-height: 100vh;
    font-family: 'Inter', sans-serif;
    padding-bottom: 2rem;
  }

    .profile-card {
    background: rgba(255, 255, 255, 0.85);
    backdrop-filter: blur(12px);
    border-radius: 20px;
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.08);
    padding: 2.5rem 3rem;
    margin-top: 3rem;
    margin-bottom: 3rem;
    max-width: 1000px;
    transition: all 0.3s ease;
  }

    .profile-card:hover {
    box-shadow: 0 18px 32px rgba(0, 0, 0, 0.12);
  }

    .profile-section-header {
    font-size: 1.75rem;
    font-weight: 700;
    margin-bottom: 2rem;
    border-left: 6px solid #4f9aff;
    padding-left: 1rem;
    color: #333;
  }

    .profile-details h4 {
    font-size: 1.1rem;
    font-weight: 600;
    margin-top: 1rem;
    color: #333;
  }

    .profile-details a {
    font-weight: 500;
    font-size: 0.95rem;
  }

    .profile-picture-container {
    background-color: #f7f9fc;
    border: 1px solid #e0e0e0;
    padding: 1.5rem;
    border-radius: 16px;
    text-align: center;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.03);
  }

    .profile-picture {
    width: 150px;
    height: 150px;
    border-radius: 50%;
    object-fit: cover;
    border: 3px solid #eee;
    background-color: #f0f0f0;
    margin-bottom: 1rem;
  }

    .btn-primary {
    background-color: #4f9aff;
    border: none;
    font-weight: 600;
  }

    .btn-primary:hover {
    background-color: #3a85e0;
  }

    .edit-link {
    margin-left: 0.5rem;
    font-size: 0.95rem;
    font-weight: 500;
  }

    h4 {
    margin-top: 1.5rem;
  }

  /* Skill Tag Styling */
    .skill-tag {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background-color: #f1f5ff;
    border-radius: 12px;
    padding: 0.75rem 1rem;
    margin-bottom: 0.75rem;
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.04);
  }

    .skill-name {
    font-weight: 600;
    font-size: 1rem;
    color: #333;
  }

    .skill-level {
    font-size: 0.95rem;
    font-weight: 500;
    color: #4f9aff;
    margin-left: 0.5rem;
  }

    .skill-tag .btn-sm {
    margin-left: 1rem;
  }

    #skills-list {
    padding-left: 0;
    list-style-type: none;
  }


    .skill-buffer {
    font-weight: 300;
    margin-right: 0.5rem;
  }

  .scroll-footer {
  color: #f0f0f0 !important;  /* Soft off-white */
  background-color: #222 !important;  /* Ensure background matches */
  font-size: 0.9rem;
}


</style>
